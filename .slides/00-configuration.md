---
title: 'Glides demo deck'
styles:
  accent: '#f0c050'
  accent2: '#e06848'
  bg: '#0c0c0f'
  fg: '#e8e6e1'
  body: '#c4c1ba'
---

# Configuration preview

**Body** copy, *accent* emphasis, [link](https://github.com), and `inline code`. List markers use **accent**; blockquotes use **dim** with an **accent** border.

- First item
- Second item

> Short quote.

```javascript
function hello(name) {
  return `Hello, ${name}!`;
}
```

Tweak `styles:` above (see README) and refresh.
