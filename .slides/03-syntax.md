## Supported Syntax

You get **bold**, *emphasis*, `inline code`, lists, blockquotes, headings, and fenced code blocks:

```javascript
const message = "Hello from Slides!";
console.log(message);
```

### Everything you need, nothing you don't.
