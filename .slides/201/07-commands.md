# Commands

### Scoped instructions you trigger on purpose

Commands (`.cursor/commands`) package **prompts plus optional context** so the model doesn't guess the job.

- **Narrows the window**: you describe the task once, share only matching rules or files
- **Good design**: one command, one outcome; keep body text tight
- **Bad design**: "load everything just in case" turns every run into a firehose
