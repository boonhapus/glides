# Commands

### Scoped instructions triggered intentionally

Commands bundle prompts with the exact context for a defined unit of work.

- By scoping the request to necessary files and rules, **commands maximize accuracy**.
- **Good design**: one command, one outcome; keep body text targeted to the goal.
- **Bad design**: loading everything "just in case" creates noise that dilutes the prompt.

`.cursor/commands`
